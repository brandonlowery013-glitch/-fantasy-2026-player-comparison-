import fs from 'node:fs';
import path from 'node:path';
const root=process.cwd();
const read=p=>JSON.parse(fs.readFileSync(path.join(root,p),'utf8'));
const c=read('data/sources/automatic-football-context-adapters-2026.json');
const collector=fs.readFileSync(path.join(root,'scripts/collect-automatic-football-context.mjs'),'utf8');
const blocked=[];
const required=['role','injury','team_environment','opponent','qb_context'];
if(c.status!=='STEP_10_AUTOMATIC_CONTEXT_ADAPTERS_LOCKED')blocked.push('unexpected status');
if(c.mode!=='SHADOW_ONLY'||c.actionable!==false)blocked.push('adapter layer must remain SHADOW_ONLY/non-actionable');
if(c.sportsbook_inputs_allowed!==false)blocked.push('sportsbook inputs must be prohibited');
for(const t of required){const a=c.adapters?.[t];if(!a)blocked.push(`missing adapter ${t}`);else{if(!a.automated_source)blocked.push(`${t} missing automated_source`);if(a.numeric_adjustments_calibrated!==false)blocked.push(`${t} must not claim calibrated numeric adjustments`);}}
if(c.write_contract?.append_only!==true)blocked.push('snapshot ledger must be append only');
if(c.write_contract?.dedupe_by_evidence_fingerprint!==true)blocked.push('evidence fingerprint dedupe required');
for(const t of required)if(!(c.write_contract?.allowed_signal_types||[]).includes(t))blocked.push(`write contract missing ${t}`);
const a=c.availability_contract||{};
if(a.depth_chart_presence_may_support_expected_active_true!==false)blocked.push('plain depth-chart presence must not imply active');
if(a.explicit_espn_athlete_active_flag_may_support_availability!==true)blocked.push('explicit ESPN athlete active flag contract missing');
if(a.injury_full_practice_or_explicit_active_may_support_expected_active_true!==true)blocked.push('explicit positive injury/practice evidence contract missing');
if(a.absence_from_injury_feed_never_by_itself_implies_active!==true)blocked.push('missing no-injury-absence safeguard');
if(a.injury_out_ir_pup_suspended_overrides_active!==true)blocked.push('missing explicit inactive override');
const w=c.week_alignment_contract||{};
if(w.verified_schedule_week_is_primary!==true||w.espn_scoreboard_scan_is_fallback_only!==true||w.unresolved_week_must_block!==true)blocked.push('verified schedule week alignment contract missing');
if(!collector.includes('function verifiedScheduleWeek()')||!collector.includes('const scheduled=verifiedScheduleWeek();if(scheduled!=null)return scheduled'))blocked.push('collector does not prioritize verified schedule week');
if(collector.includes("availability_basis:expected===true?'current_depth_chart_presence'"))blocked.push('collector still treats depth-chart presence as active evidence');
if(!collector.includes('espn_explicit_athlete_active_flag'))blocked.push('collector does not preserve explicit athlete active flag basis');
if(!collector.includes("throw new Error('Unable to resolve 2026 NFL week from verified schedule or ESPN')"))blocked.push('collector still permits unresolved week fallback');
const report={generated_at:new Date().toISOString(),result:blocked.length?'BLOCKED':'PASS',status:c.status,mode:c.mode,actionable:c.actionable,sportsbook_inputs_allowed:c.sportsbook_inputs_allowed,adapters:required,blocked};
fs.mkdirSync(path.join(root,'guardrails'),{recursive:true});fs.writeFileSync(path.join(root,'guardrails/automatic-football-context-adapters-contract-report.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(blocked.length)process.exit(1);
