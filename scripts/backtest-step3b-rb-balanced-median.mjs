import fs from 'node:fs';
const seasons=[2017,2018,2019,2020,2021,2022,2023,2024,2025],targets=[2021,2022,2023,2024,2025];
const n=v=>Number.isFinite(Number(v))?Number(v):0,avg=a=>a.length?a.reduce((s,x)=>s+x,0)/a.length:null;
const q=(a,p)=>{const x=a.filter(Number.isFinite).sort((a,b)=>a-b);if(!x.length)return null;const z=(x.length-1)*p,l=Math.floor(z),h=Math.ceil(z);return l===h?x[l]:x[l]+(x[h]-x[l])*(z-l)};
function parseCsv(t){const rs=[];let row=[],f='',qq=false;for(let i=0;i<t.length;i++){const c=t[i];if(qq){if(c==='"'&&t[i+1]==='"'){f+='"';i++;}else if(c==='"')qq=false;else f+=c;}else{if(c==='"')qq=true;else if(c===','){row.push(f);f='';}else if(c==='\n'){row.push(f.replace(/\r$/,''));rs.push(row);row=[];f='';}else f+=c;}}if(f.length||row.length){row.push(f);rs.push(row)}const h=rs.shift()||[];return rs.filter(r=>r.length>1).map(r=>Object.fromEntries(h.map((k,i)=>[k,r[i]??''])))}
async function csv(u){const r=await fetch(u,{headers:{'user-agent':'fantasy-2026-probability-pipeline'}});if(!r.ok)throw new Error(`${r.status} ${u}`);return parseCsv(await r.text())}
const rows=[];for(const season of seasons){const u=`https://github.com/nflverse/nflverse-data/releases/download/stats_player/stats_player_week_${season}.csv`;for(const r of await csv(u)){if(String(r.season_type||'REG').toUpperCase()!=='REG'||String(r.position||'').toUpperCase()!=='RB')continue;const id=r.player_id||r.gsis_id;if(!id)continue;const fp=n(r.receptions)+n(r.receiving_yards)*.1+n(r.receiving_tds)*6+n(r.rushing_yards)*.1+n(r.rushing_tds)*6;rows.push({id:String(id),player:r.player_display_name||r.player_name||String(id),season,team:r.recent_team||r.team||null,fp});}}
const by=new Map();for(const r of rows){const k=`${r.id}|${r.season}`;if(!by.has(k))by.set(k,[]);by.get(k).push(r)}const ss=[];for(const [k,rs] of by){const [id,ys]=k.split('|'),season=+ys;if(rs.length<4)continue;ss.push({id,player:rs[0].player,season,games:rs.length,team:[...new Set(rs.map(x=>x.team).filter(Boolean))].join('+'),ppg:avg(rs.map(x=>x.fp))});}const map=new Map(ss.map(x=>[`${x.id}|${x.season}`,x]));
const ex=[];for(const cur of ss){if(cur.season<2018||cur.games<6)continue;const pr=map.get(`${cur.id}|${cur.season-1}`);if(!pr||pr.games<4)continue;ex.push({...cur,prior:pr,resid:cur.ppg-pr.ppg});}
function base(train,x){if(train.length<40)return null;return {q10:x.prior.ppg+q(train.map(z=>z.resid),.1),q50:x.prior.ppg+q(train.map(z=>z.resid),.5),q90:x.prior.ppg+q(train.map(z=>z.resid),.9)}}
const methods=['TRAILING_2','TRAILING_3','EXPANDING','BLEND_T2_EXP_25','BLEND_T2_EXP_50','BLEND_T2_EXP_75','BLEND_T3_EXP_50'];
const rec=Object.fromEntries(methods.map(m=>[m,[]]));
for(const target of targets){const train=ex.filter(x=>x.season<target);const t2=ex.filter(x=>x.season>=target-2&&x.season<target).map(x=>x.resid);const t3=ex.filter(x=>x.season>=target-3&&x.season<target).map(x=>x.resid);const exp=train.map(x=>x.resid);const c2=q(t2,.5),c3=q(t3,.5),ce=q(exp,.5);for(const x of ex.filter(z=>z.season===target)){const b=base(train,x);if(!b)continue;const corr={TRAILING_2:c2,TRAILING_3:c3,EXPANDING:ce,BLEND_T2_EXP_25:.25*c2+.75*ce,BLEND_T2_EXP_50:.5*c2+.5*ce,BLEND_T2_EXP_75:.75*c2+.25*ce,BLEND_T3_EXP_50:.5*c3+.5*ce};for(const m of methods){const raw=x.prior.ppg+corr[m],med=Math.min(b.q90,Math.max(b.q10,raw));rec[m].push({...x,target,q10:b.q10,q90:b.q90,q50:med,raw_q50:raw,clipped:med!==raw});}}}
function compat(obs,p,N){return N>0&&Math.abs(obs-p)<=1.96*Math.sqrt(p*(1-p)/N)}
function summarize(a){const N=a.length;if(!N)return{n:0,q50_coverage:null,absolute_error:null,compatible_95pct:false,mean_gap:null};const c=a.filter(x=>x.ppg<=x.q50).length/N;return{n:N,q50_coverage:c,absolute_error:Math.abs(c-.5),compatible_95pct:compat(c,.5,N),mean_gap:avg(a.map(x=>x.ppg-x.q50))}}
const results={};for(const m of methods){const all=rec[m],overall=summarize(all),by_year={};for(const y of targets)by_year[y]=summarize(all.filter(x=>x.target===y));const yearMae=avg(Object.values(by_year).map(x=>x.absolute_error));const allYearsPass=Object.values(by_year).every(x=>x.compatible_95pct);const tailQ10=all.filter(x=>x.ppg<=x.q10).length/all.length,tailQ90=all.filter(x=>x.ppg<=x.q90).length/all.length;results[m]={overall,by_year,mean_year_abs_error:yearMae,all_years_q50_compatible_95pct:allYearsPass,tail_q10:tailQ10,tail_q90:tailQ90,clipped_count:all.filter(x=>x.clipped).length,pass:overall.compatible_95pct&&allYearsPass};}
const ranked=methods.map(m=>({method:m,pass:results[m].pass,score:results[m].mean_year_abs_error,overall_error:results[m].overall.absolute_error})).sort((a,b)=>Number(b.pass)-Number(a.pass)||a.score-b.score||a.overall_error-b.overall_error);const winner=ranked.find(x=>x.pass)||null;
const report={generated_at:new Date().toISOString(),step:'STEP3B_RB_BALANCED_MEDIAN_BACKTEST',history_window:seasons,evaluation_targets:targets,methods,selection_rule:'Leakage-safe preseason-only residual corrections. Winner must be 95% Q50-compatible overall and in every held-out year; lowest mean yearly absolute Q50 coverage error breaks ties. Blends shrink recent residual correction toward the expanding historical residual median to reduce year-to-year whipsaw. Q10/Q90 stay unchanged.',status:winner?'CANDIDATE_PASSED_REQUIRES_REVIEW':'NO_CANDIDATE_PASSED_CONTINUE_TESTING',winner:winner?.method||null,ranking:ranked,results,sportsbook_or_adp_used:false,target_season_outcome_used_for_prediction:false,live_weight:0,live_projection_movement:0,live_rank_movement:0,promotion_allowed:false};
fs.mkdirSync('guardrails',{recursive:true});fs.writeFileSync('guardrails/step3b-rb-balanced-median.json',JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));