import fs from 'node:fs';
const path='guardrails/current-football-review.json';const ledger=JSON.parse(fs.readFileSync(path,'utf8'));const START=Date.parse(process.env.PLAYER_NEWS_RSS_START||new Date(Date.now()-14*86400000).toISOString()),END=Date.parse(process.env.PLAYER_NEWS_RSS_END||new Date().toISOString()),MAX=Number(process.env.PLAYER_NEWS_RSS_MAX||20);const sleep=ms=>new Promise(r=>setTimeout(r,ms));const decode=s=>String(s||'').replace(/<!\[CDATA\[|\]\]>/g,'').replace(/&amp;/g,'&').replace(/&quot;/g,'"').replace(/&#39;/g,"'").replace(/&lt;/g,'<').replace(/&gt;/g,'>');const strip=s=>decode(String(s||'').replace(/<[^>]+>/g,' ')).replace(/\s+/g,' ').trim();const norm=s=>strip(s).toLowerCase().normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/\b(jr|sr|ii|iii|iv)\b/g,'').replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim();const majorDomains=['latimes.com','nytimes.com','washingtonpost.com','apnews.com','reuters.com','espn.com','cbssports.com','nbcsports.com','foxsports.com','si.com','yahoo.com','usatoday.com','nfl.com','theathletic.com'];function items(xml){const out=[];for(const m of xml.matchAll(/<item>([\s\S]*?)<\/item>/g)){const x=m[1],sm=x.match(/<source(?:\s+url="([^"]+)")?>([\s\S]*?)<\/source>/);out.push({title:strip((x.match(/<title>([\s\S]*?)<\/title>/)||[])[1]),link:strip((x.match(/<link>([\s\S]*?)<\/link>/)||[])[1]),pub:strip((x.match(/<pubDate>([\s\S]*?)<\/pubDate>/)||[])[1]),desc:strip((x.match(/<description>([\s\S]*?)<\/description>/)||[])[1]),sourceUrl:sm?.[1]||'',sourceName:strip(sm?.[2]||'')});}return out;}async function get(url){const r=await fetch(url,{headers:{'user-agent':'Mozilla/5.0 (compatible; Fantasy2026PlayerNews/1.1)','accept':'application/rss+xml,text/xml'}});if(!r.ok)throw new Error(`${r.status}`);return r.text();}
let fetched=0,added=0,failures=[];for(const p of ledger.players||[]){p.retroactive_player_news_search={attempted:true,start:new Date(START).toISOString(),end:new Date(END).toISOString(),status:'PENDING'};const url=`https://news.google.com/rss/search?q=${encodeURIComponent(`\"${p.player}\" NFL`)}&hl=en-US&gl=US&ceid=US:en`;try{const xml=await get(url);fetched++;const phrase=norm(p.player),rows=items(xml).filter(x=>{const t=Date.parse(x.pub||'');return Number.isFinite(t)&&t>=START&&t<=END+86400000&&norm(`${x.title} ${x.desc}`).includes(phrase);}).slice(0,MAX);p.external_news_mentions=p.external_news_mentions||[];for(const x of rows){const host=(()=>{try{return new URL(x.sourceUrl).hostname.replace(/^www\./,'');}catch{return'';}})(),tier=majorDomains.some(d=>host===d||host.endsWith('.'+d))?'MAJOR_MEDIA':'DISCOVERY_ONLY',m={source:'GOOGLE_NEWS_RSS',publisher:x.sourceName,publisher_url:x.sourceUrl||null,source_tier:tier,headline:x.title,description:x.desc,published:new Date(x.pub).toISOString(),url:x.link,context_scope:'DIRECT_PLAYER_EXTERNAL_DISCOVERY',player_bound:true,proposal_only:true,retroactive_source:true};if(!p.external_news_mentions.some(y=>y.headline===m.headline&&y.publisher===m.publisher)){p.external_news_mentions.push(m);added++;}}p.retroactive_player_news_search.status='CHECKED';p.retroactive_player_news_search.matches=rows.length;}catch(e){p.retroactive_player_news_search.status='SOURCE_FAILURE';failures.push(`${p.player}: ${e.message}`);}await sleep(20);}ledger.source_quality={...(ledger.source_quality||{}),player_external_news_rss:true,player_external_news_rss_start:new Date(START).toISOString(),player_external_news_rss_end:new Date(END).toISOString(),player_external_news_rss_players_fetched:fetched,player_external_news_rss_mentions_added:added,player_external_news_rss_failures:failures.slice(0,30),player_external_news_rss_governance:'DISCOVERY_ONLY_REQUIRES_PLAYER_BINDING; MAJOR_MEDIA MAY SUPPORT RETROACTIVE REVIEW; NO AUTOMATIC SCORE CHANGE'};fs.writeFileSync(path,JSON.stringify(ledger,null,2)+'\n');console.log(JSON.stringify({result:'PASS',players_fetched:fetched,mentions_added:added,failures:failures.length},null,2));
