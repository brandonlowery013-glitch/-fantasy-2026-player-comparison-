import fs from 'node:fs';
import path from 'node:path';

const root=process.cwd();
const read=p=>JSON.parse(fs.readFileSync(path.join(root,p),'utf8'));
const write=(p,x)=>{const f=path.join(root,p);fs.mkdirSync(path.dirname(f),{recursive:true});fs.writeFileSync(f,JSON.stringify(x,null,2)+'\n');};
const report=read('analysis/transition-intelligence-current.json');
const ledger=read('guardrails/current-football-review.json');
const source=read('MODEL_SOURCE_OF_TRUTH.json');
const phaseConfig=read(`config/season-phase-${source.season||2026}.json`);
const campStart=Date.parse(phaseConfig.retroactive_camp.start),campEnd=Date.parse(phaseConfig.retroactive_camp.end),currentStart=Date.parse(phaseConfig.current_season.start);
if(![campStart,campEnd,currentStart].every(Number.isFinite)||campEnd>=currentStart)throw new Error('Invalid season phase boundaries');
const byLedger=new Map((ledger.players||[]).map(x=>[x.player,x]));
const norm=s=>String(s||'').toLowerCase().normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/\b(jr|sr|ii|iii|iv)\b/g,'').replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim();

const physicalHealth=/\b(injur(?:y|ed)|soreness|sprain|strain|tear|fracture|surgery|rehab|recovery|recovering|swollen|acl|lcl|mcl|meniscus|achilles|hamstring|ankle|knee|shoulder|back|groin|quad|calf|foot|wrist|hand|concussion|illness|sick|pup|nfi|\bir\b|setback|recurrence)\b/i;
const practiceRestriction=/\b(limited (?:in|at) practice|missed practice|did not practice|left practice|exits? practice)\b/i;
const adverseHealth=/\b(new injury|suffer(?:ed|s)?|injured at practice|injured in practice|dealing with|diagnosed with|out\b|ruled out|will miss|expected to miss|miss(?:ed|es|ing) practice|did not practice|limited (?:in|at) practice|left practice|exits? practice|placed on (?:ir|pup|nfi)|setback|recurrence|swollen|soreness|sprain|strain|tear|fracture|surgery|torn|ruptured|not ready|week[- ]to[- ]week|day[- ]to[- ]day)\b/i;
const nonHealthAbsence=/\b(personal reasons?|personal matter|family matter|family reasons?|excused absence|veteran rest|rest day|maintenance day|load management|not injury related|not injury-related|non injury related|non-injury-related)\b/i;
const positiveRole=/\b(won|named starter|starter|starting|lead|featured|feature back|first team|with the ones|bulk reps|expanded role|increase(?:d)? work|more work|more reps|trusted|ahead|target share increased|role grew)\b/i;
const positiveRecovery=/\b(full practice|full participant|cleared|activated|healthy|ready|returned to practice|returns to practice|return to practice|back in action|back at practice|off (?:the )?injury report|totally fine|not serious|no limitations?|without limitation|ramping up well|progressing|on track(?: to be)?(?: ready)?|expected to be ready|will be ready|set to play|will play|avoids? injury|escaped injury|good to go|encouraging (?:injury )?update|good news|confidence and explosion back|hit all (?:of )?his checkpoints)\b/i;
const negativeRole=/\b(lost .*job|backup|behind|reduced role|fewer reps|losing snaps|losing targets|demoted|waived|released)\b/i;
const neutral=/\b(learning|install|competition|battle|split|rotation|working through|adjust|transition|new offense|new scheme|new coordinator|new quarterback|new qb)\b/i;
const roleTerms=/\b(starter|starting|backup|lead|feature|first team|with the ones|reps|snap|route|target|carry|touch|workload|role|depth chart|competition|committee|split|rotation|bulk reps|more work)\b/i;
const recoveryTerms=/\b(recover|rehab|surgery|acl|mcl|lcl|meniscus|achilles|ankle|knee|hamstring|cleared|practice|pup|ir|limited|full participant|healthy|setback|swollen)\b/i;
const schemeTerms=/\b(offense|offensive coordinator|coordinator|scheme|system|playbook|install|motion|rpo|play action|under center|shotgun|terminology|concept)\b/i;
const chemistryTerms=/\b(chemistry|rapport|connection|timing|trust|sync|working with)\b/i;
const roundupBoundary=/\b(signings?|injuries?|transactions?|roster moves?|roster updates?|waivers?|cuts?|releases?|activations?|preseason news)\b/i;

function playerSections(e,player){const p=norm(player);if(!p)return[];const out=[];for(const raw of [e.headline,e.description,e.matched_context,e.body_text].filter(Boolean)){const pieces=String(raw).split(roundupBoundary),matching=pieces.filter(piece=>norm(piece).includes(p));if(matching.length)for(const piece of matching)out.push(norm(piece));else{const n=norm(raw);if(n.includes(p))out.push(n);}}return[...new Set(out)];}
function playerLocalText(e,player,radius=82){const p=norm(player);if(!p)return'';const out=[];for(const t of playerSections(e,player)){let from=0;while(out.length<12){const i=t.indexOf(p,from);if(i<0)break;out.push(t.slice(Math.max(0,i-radius),Math.min(t.length,i+p.length+radius)));from=i+p.length;}}return norm(out.join(' '));}
function ownHealth(e,player){const p=norm(player),t=playerLocalText(e,player,58);if(!p||!t.includes(p))return false;const clean=t.replace(/\b(?:not|non)\s+injury\s+related\b/gi,' ');const physical=physicalHealth.test(clean);if(nonHealthAbsence.test(t)&&!physical)return false;if(practiceRestriction.test(t)&&!physical)return false;return physical;}
function ts(e){const t=Date.parse(e.published||'');return Number.isFinite(t)?t:null;}
function phase(t){if(!t)return'UNDATED';if(t>=currentStart)return'CURRENT_SEASON_STATE';if(t<campStart)return'PRE_CAMP_OFFSEASON';const d=new Date(t),m=d.getUTCMonth()+1,day=d.getUTCDate();if(m<=5)return'OFFSEASON';if(m===6)return'MINICAMP_OTAS';if(m===7)return'TRAINING_CAMP_EARLY';if(m===8&&day<=15)return'TRAINING_CAMP_MID';if(m===8&&day<=26)return'PRESEASON_LATE_CAMP';return'ROSTER_CUTS_FINAL_CAMP';}
function direction(e,player,local){
  const roleGain=positiveRole.test(local),recoveryGain=positiveRecovery.test(local),roleLoss=negativeRole.test(local),selfHealth=ownHealth(e,player),adverse=selfHealth&&adverseHealth.test(local);
  if(recoveryGain&&!roleLoss&&!(/\b(setback|recurrence|will miss|expected to miss|ruled out|placed on (?:ir|pup|nfi))\b/i.test(local)))return'POSITIVE';
  if(roleLoss&&!roleGain)return'NEGATIVE';
  if(adverse)return'NEGATIVE';
  if(roleGain)return'POSITIVE';
  // Injury/recovery/history mentioned without a current adverse or resolving state is context, not a negative signal.
  if(selfHealth)return'CONTEXT';
  if(neutral.test(local))return'DEVELOPMENTAL_NEUTRAL';
  return'CONTEXT';
}
function dimensions(local,selfHealth){const out=[];if(roleTerms.test(local))out.push('role_usage');if(selfHealth||recoveryTerms.test(local))out.push('availability_recovery');if(schemeTerms.test(local))out.push('scheme_adaptation');if(chemistryTerms.test(local))out.push('chemistry');return out;}
function significance(e,local,selfHealth){let score=0;if(e.direct_player_evidence)score+=4;if(e.cluster_trigger)score+=2;if(roleTerms.test(local))score+=3;if(selfHealth||recoveryTerms.test(local))score+=3;if(/\b(named starter|won .* job|activated|placed on ir|waived|released|traded|signed|full practice|full participant|cleared|bulk reps|more work)\b/i.test(local))score+=4;if(schemeTerms.test(local)||chemistryTerms.test(local))score+=1;return score;}
function trajectory(events){const dated=events.filter(x=>x.timestamp!==null);if(!dated.length)return'UNKNOWN';const meaningful=dated.filter(x=>x.significance>=3),early=(meaningful[0]||dated[0]),late=(meaningful.at(-1)||dated.at(-1));if(early.direction!==late.direction){if(late.direction==='POSITIVE')return'IMPROVING_OR_ROLE_GAIN';if(late.direction==='NEGATIVE')return'DECLINING_OR_ROLE_LOSS';return'EVOLVING_MIXED';}if(late.direction==='POSITIVE')return'POSITIVE_CONFIRMED';if(late.direction==='NEGATIVE')return'NEGATIVE_CONFIRMED';return'STABLE_OR_CONTEXT_ONLY';}
function bestCurrent(events){const current=events.filter(x=>x.timestamp!==null&&x.timestamp>=currentStart);if(!current.length)return null;const meaningful=current.filter(x=>x.significance>=3);return[...(meaningful.length?meaningful:current)].sort((a,b)=>b.significance-a.significance||b.timestamp-a.timestamp)[0];}
function bestCampBaseline(events){const camp=events.filter(x=>x.timestamp!==null&&x.timestamp>=campStart&&x.timestamp<=campEnd);if(!camp.length)return null;const meaningful=camp.filter(x=>x.significance>=3);return[...(meaningful.length?meaningful:camp)].sort((a,b)=>b.timestamp-a.timestamp||b.significance-a.significance)[0];}

const wilsonLocal=playerLocalText({matched_context:'wr michael wilson signed extension injuries wr zay flowers left practice with knee injury'},'Michael Wilson');if(ownHealth({matched_context:wilsonLocal},'Michael Wilson'))throw new Error('Regression: Michael Wilson inherited neighboring injury');
const goffEvent={matched_context:'cb ennis rakestraw left practice with lower leg injury preseason news qb jared goff will not play saturday per coach'};const goffLocal=playerLocalText(goffEvent,'Jared Goff');if(ownHealth(goffEvent,'Jared Goff')||direction(goffEvent,'Jared Goff',goffLocal)==='NEGATIVE')throw new Error('Regression: Goff inherited neighboring injury or preseason rest negative');
const bowersEvent={matched_context:'te brock bowers missed practice for personal reasons'};const bowersLocal=playerLocalText(bowersEvent,'Brock Bowers');if(ownHealth(bowersEvent,'Brock Bowers')||direction(bowersEvent,'Brock Bowers',bowersLocal)==='NEGATIVE')throw new Error('Regression: Bowers personal absence treated as injury');
const walkerEvent={matched_context:'kenneth walker is dealing with a swollen ankle and missed practice'};const walkerLocal=playerLocalText(walkerEvent,'Kenneth Walker III');if(direction(walkerEvent,'Kenneth Walker III',walkerLocal)!=='NEGATIVE')throw new Error('Regression: Walker own injury lost');
const mahomesEvent={matched_context:'patrick mahomes returned to full practice after acl lcl rehab and is on track for week one'};const mahomesLocal=playerLocalText(mahomesEvent,'Patrick Mahomes II');if(direction(mahomesEvent,'Patrick Mahomes II',mahomesLocal)!=='POSITIVE')throw new Error('Regression: Mahomes resolved recovery not positive');
const shakirEvent={matched_context:'khalil shakir gets encouraging injury update and is expected to be ready'};const shakirLocal=playerLocalText(shakirEvent,'Khalil Shakir');if(direction(shakirEvent,'Khalil Shakir',shakirLocal)!=='POSITIVE')throw new Error('Regression: encouraging recovery still negative');
const qjEvent={matched_context:'quentin johnston injury update after scare'};const qjLocal=playerLocalText(qjEvent,'Quentin Johnston');if(direction(qjEvent,'Quentin Johnston',qjLocal)==='NEGATIVE')throw new Error('Regression: generic injury update became negative without adverse fact');
const danielsEvent={matched_context:'jayden daniels looks back on injury plagued 2025 and how it can help him'};const danielsLocal=playerLocalText(danielsEvent,'Jayden Daniels');if(direction(danielsEvent,'Jayden Daniels',danielsLocal)==='NEGATIVE')throw new Error('Regression: historical injury context became current negative');
const waddleEvent={matched_context:'jaylen waddle gets encouraging injury update from sean payton'};const waddleLocal=playerLocalText(waddleEvent,'Jaylen Waddle');if(direction(waddleEvent,'Jaylen Waddle',waddleLocal)!=='POSITIVE')throw new Error('Regression: encouraging injury update not positive');

let chronologyPlayers=0,currentPlayers=0,campEvidencePlayers=0,campNoEvidencePlayers=0;
for(const row of report.rows||[]){const events=(row.development_evidence||[]).map(e=>{const t=ts(e),local=playerLocalText(e,row.player),selfHealth=ownHealth(e,row.player);return{...e,timestamp:t,phase:phase(t),direction:direction(e,row.player,local),dimensions:dimensions(local,selfHealth),significance:significance(e,local,selfHealth),chronology_context_text:local}}).sort((a,b)=>(a.timestamp??Number.MAX_SAFE_INTEGER)-(b.timestamp??Number.MAX_SAFE_INTEGER));const campEvents=events.filter(x=>x.timestamp!==null&&x.timestamp>=campStart&&x.timestamp<=campEnd),currentEvents=events.filter(x=>x.timestamp!==null&&x.timestamp>=currentStart);const current=bestCurrent(events),campBaseline=bestCampBaseline(events);const basis=x=>x?{published:x.published,phase:x.phase,direction:x.direction,headline:x.headline,dimensions:x.dimensions,categories:x.categories||[]}:null;const campAudit={required:true,window:{start:phaseConfig.retroactive_camp.start,end:phaseConfig.retroactive_camp.end},status:campEvents.length?'RETROACTIVE_CAMP_EVIDENCE_FOUND':'RETROACTIVE_CAMP_REVIEWED_NO_EVIDENCE',event_count:campEvents.length,trajectory:trajectory(campEvents),latest_camp_basis:basis(campBaseline),authority:phaseConfig.retroactive_camp.authority,rule:phaseConfig.retroactive_camp.rule};const currentState={required:true,window:{start:phaseConfig.current_season.start,end:report.generated_at||new Date().toISOString()},status:current?'CURRENT_EVIDENCE_FOUND':'CURRENT_REVIEWED_NO_POST_CAMP_EVIDENCE',event_count:currentEvents.length,current_state_basis:basis(current),inherited_camp_baseline:!current?basis(campBaseline):null,authority:phaseConfig.current_season.authority,rule:phaseConfig.current_season.rule};row.chronological_development={mandatory:true,event_count:events.length,events,camp_retroactive_audit:campAudit,current_season_state:currentState,overall_trajectory:trajectory(events),rule:'DIRECTION REQUIRES PLAYER-LOCAL FACTUAL STATE: EXPLICIT RESOLUTION IS POSITIVE, EXPLICIT ADVERSE STATE IS NEGATIVE, GENERIC/HISTORICAL INJURY CONTEXT IS CONTEXT; CAMP IS CLOSED HISTORY; CURRENT-SEASON EVIDENCE IS AUTHORITATIVE'};if(events.length)chronologyPlayers++;if(current)currentPlayers++;if(campEvents.length)campEvidencePlayers++;else campNoEvidencePlayers++;const l=byLedger.get(row.player);if(l?.transition_intelligence)l.transition_intelligence.chronological_development=row.chronological_development;}
report.schema_version='1.4.3';report.season_phase={config:`config/season-phase-${source.season||2026}.json`,retroactive_camp:phaseConfig.retroactive_camp,current_season:phaseConfig.current_season};report.chronological_context={mandatory:true,players_with_timeline:chronologyPlayers,players_with_current_state_basis:currentPlayers,retroactive_camp_players_with_evidence:campEvidencePlayers,retroactive_camp_players_reviewed_no_evidence:campNoEvidencePlayers,precedence:'CURRENT_SEASON_STATE_OVER_CLOSED_CAMP_STATE',player_local_scoring_required:true,recovery_resolution_precedence:true,adverse_fact_required_for_negative_health:true,rule:'Retroactively reconstruct camp as closed history. A health mention is not negative without a player-local adverse fact; explicit recovery is positive; post-camp evidence controls current state.'};ledger.transition_intelligence_schema={...(ledger.transition_intelligence_schema||{}),version:'1.4.3',chronological_context_mandatory:true,retroactive_camp_audit_mandatory:true,current_season_state_mandatory:true,season_phase_config:report.season_phase.config,chronology_rule:report.chronological_context.rule};
write('analysis/transition-intelligence-current.json',report);write('guardrails/current-football-review.json',ledger);if((report.rows||[]).some(r=>r.chronological_development?.camp_retroactive_audit?.required!==true))throw new Error('RETROACTIVE_CAMP_AUDIT_MISSING');if((report.rows||[]).some(r=>r.chronological_development?.current_season_state?.required!==true))throw new Error('CURRENT_SEASON_STATE_REVIEW_MISSING');console.log(JSON.stringify({result:'PASS',players:(report.rows||[]).length,players_with_timeline:chronologyPlayers,current_state_players:currentPlayers,retroactive_camp_evidence_players:campEvidencePlayers,retroactive_camp_no_evidence_players:campNoEvidencePlayers},null,2));
