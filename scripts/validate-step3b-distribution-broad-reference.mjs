import fs from 'node:fs';const p='guardrails/step3b-distribution-broad-reference.json';if(!fs.existsSync(p))throw new Error('missing broad-reference distribution report');const r=JSON.parse(fs.readFileSync(p,'utf8'));const need=(x,m)=>{if(!x)throw new Error(m)};need(r.sportsbook_or_adp_used===false,'market contamination');need(r.live_weight===0&&r.live_projection_movement===0&&r.live_rank_movement===0,'live authority');need(r.promotion_allowed===false,'promotion blocked');need(r.reference_unique_players>=500,'broad reference too small');need(r.player_season_examples>=1000,'player-season sample too small');need(r.heldout_predictions>=500,'heldout sample too small');for(const p of ['QB','RB','WR','TE']){const x=r.by_position?.[p];need(x?.n>=50,`insufficient ${p}`);for(const k of ['q10','q50','q90','central80'])need(Number.isFinite(x[k]?.coverage),`missing ${p} ${k}`)}console.log(`Step 3B broad-reference distribution robustness PASS — ${r.status}`);