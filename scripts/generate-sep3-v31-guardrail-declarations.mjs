// PR-scoped guardrail declarations: prefer the newest governed canonical payload, retain older compatibility modes otherwise.
import fs from 'node:fs';
const R=p=>JSON.parse(fs.readFileSync(p,'utf8')),W=(p,x)=>fs.writeFileSync(p,JSON.stringify(x,null,2)+'\n');
const src=R('MODEL_SOURCE_OF_TRUTH.json'),patch=R(src.current_update_layer);
let players=[];for(let i=0;i<src.runtime_player_shards;i++)players.push(...R(`players${i}.json`));players=players.map(p=>({...p,...(patch.players?.[p.n]||{})}));
if(players.length!==src.active_player_model)throw new Error('declaration universe gate');
const by=new Map(players.map(p=>[p.n,p]));
const postInput='analysis/post-overall-audit-canonical-apply-input-2026-09-05.json';
const postQa='analysis/post-overall-audit-canonical-apply-qa-2026-09-05.json';
const sep5Input='analysis/material-hold-canonical-apply-input-2026-09-05.json';
const sep5Qa='analysis/material-hold-canonical-apply-qa-2026-09-05.json';
const usePost=fs.existsSync(postInput)&&fs.existsSync(postQa)&&src.status==='authoritative_current_2026_09_05_post_overall_audit_recalibrated';
const useSep5=!usePost&&fs.existsSync(sep5Input)&&fs.existsSync(sep5Qa)&&src.status==='authoritative_current_2026_09_05_material_hold_recalibrated';
let model_changes=[],asOf,sourceRun=null,sourceArtifact=null,purposeSuffix,structuralReason,structuralSource,calibration;
if(usePost){
  const inp=R(postInput),qa=R(postQa);
  if(inp.approval!=='USER_APPROVED_POST_OVERALL_AUDIT_CANONICAL_APPLY'||inp.reviewed_reopens!==12||inp.changed_players!==5||inp.rows.length!==5||inp.zero_delta_resolutions.length!==7)throw new Error('post-audit declaration input gate');
  if(qa.players!==166||qa.changed!==5||qa.zero_delta!==7||qa.errors?.length||!qa.overall_unchanged||!qa.market_unchanged||!qa.projections_unchanged)throw new Error('post-audit declaration QA gate');
  const qBy=new Map(qa.changes.map(x=>[x.player,x]));
  model_changes=inp.rows.map(x=>{const p=by.get(x.player),q=qBy.get(x.player);if(!p||!q)throw new Error(`missing post-audit declaration row ${x.player}`);const expected_values={s:p.s};for(const k of Object.keys(x.component_targets))expected_values[k]=p[k];const [a,b]=String(q.tv).split('->').map(Number);if(a!==b)expected_values.tr=p.tr;return{player:x.player,reason:x.reason,source:`2026-09-05 post-Overall audit canonical apply from PR #${inp.source_pr}, merge ${inp.source_merge}; user-approved`,expected_values}});
  asOf=inp.as_of;sourceRun=inp.source_proposal_run;sourceArtifact=inp.source_proposal_artifact;purposeSuffix='Sep. 5 post-Overall audit five-player recalibration';calibration='POST_OVERALL_AUDIT_5';
  structuralReason='Advance authoritative metadata and synchronized canonical boards after the evidence-gated post-Overall audit recalibration (12 reviewed, 5 changed, 7 zero-delta).';
  structuralSource=`2026-09-05 post-Overall audit QA; source PR #${inp.source_pr}; merge ${inp.source_merge}`;
}else if(useSep5){
  const inp=R(sep5Input),qa=R(sep5Qa);if(inp.approval!=='USER_APPROVED_MATERIAL_HOLD_CALIBRATION'||inp.reviewed_holds!==19||inp.changed_players!==7||inp.rows.length!==7)throw new Error('Sep5 declaration input gate');if(qa.players!==166||qa.changed!==7||qa.reviewed_holds!==19||qa.errors?.length||!qa.overall_unchanged||!qa.market_unchanged||!qa.projections_unchanged)throw new Error('Sep5 declaration QA gate');const qBy=new Map(qa.changes.map(x=>[x.player,x]));model_changes=inp.rows.map(x=>{const p=by.get(x.player),q=qBy.get(x.player);if(!p||!q)throw new Error(`missing Sep5 declaration row ${x.player}`);const expected_values={s:p.s};for(const k of Object.keys(x.component_targets))expected_values[k]=p[k];const [a,b]=String(q.tv).split('->').map(Number);if(a!==b)expected_values.tr=p.tr;return{player:x.player,reason:x.reason,source:`2026-09-05 material-hold quantitative recalibration from PR #${inp.source_pr}, merged evidence ${inp.source_merge}; user-approved canonical apply`,expected_values}});asOf=inp.as_of;purposeSuffix='Sep. 5 material-hold quantitative recalibration';calibration='SEP5_MATERIAL_HOLDS';structuralReason='Advance authoritative metadata and synchronized canonical boards after the evidence-gated Sep. 5 material-hold quantitative recalibration (19 reviewed, 7 changed, 12 zero-delta).';structuralSource=`2026-09-05 material-hold calibration QA; source PR #${inp.source_pr}; source merge ${inp.source_merge}`;
}else{
  const inp=R('analysis/sep3-v31-canonical-apply-input.json');if(players.length!==166||inp.rows.length!==35)throw new Error('legacy Sep3 declaration universe/input gate');const core=['o','tr','tp','pr','s','pd','ce','r','e','a','rl','su','mp'];model_changes=inp.rows.map(x=>{const p=by.get(x.player);if(!p)throw new Error(`missing ${x.player}`);return{player:x.player,reason:x.reason,source:`2026-09-03 V3.1 full-universe reconciliation run ${inp.source_run}; user-approved canonical apply`,expected_values:Object.fromEntries(core.map(k=>[k,p[k]??null]))}});asOf=inp.as_of;sourceRun=inp.source_run;sourceArtifact=inp.source_artifact;purposeSuffix='Sep. 3 V3.1 35-player evidence reconciliation';calibration='SEP3_V31';structuralReason='Advance authoritative metadata and synchronized canonical boards after the user-approved Sep. 3 V3.1 35-player evidence reconciliation.';structuralSource=`2026-09-03 V3.1 run ${inp.source_run} + canonical apply QA`;
}
const oldUniverse=R('guardrails/universe-change-manifest.json');const historical_changes=(oldUniverse.changes||[]).filter(x=>x.action!=='MODEL_CHANGE');const compatibility_model_changes=model_changes.map(x=>({action:'MODEL_CHANGE',player:x.player,reason:x.reason,source:x.source,changed_fields:Object.keys(x.expected_values),expected_values:x.expected_values}));
W('guardrails/universe-change-manifest.json',{version:'1.5.0',purpose:`Historical admissions plus PR-scoped exact model-change declarations for ${purposeSuffix}.`,changes:[...historical_changes,...compatibility_model_changes],model_change_lifecycle:{status:'PR_SCOPED_EXACT_VALUES',as_of:asOf,source_run:sourceRun,source_artifact:sourceArtifact,changed_players:model_changes.length,calibration},model_changes,admission_updates:[]});
const canonical=R('canonicalBoards2026.json');if(canonical.active_players!==166||canonical.overall?.length!==166||canonical.trueValue?.length!==166)throw new Error('spreadsheet sync canonical gate');const syncFields=['n','p','t','o','pr','tr','tp','s','pd','ce','r','e','a','rl','su','mp','px','fw','st','ad'];const esc=v=>String(v??'').replaceAll('\t',' ').replaceAll('\n',' ');fs.writeFileSync('analysis/sep3-v31-spreadsheet-sync.tsv',[syncFields.join('\t'),...canonical.overall.map(p=>syncFields.map(k=>esc(p[k])).join('\t'))].join('\n')+'\n');
const oldStructural=R('guardrails/structural-change-manifest.json');let keep=(oldStructural.changes||[]).filter(x=>!['MODEL_SOURCE_OF_TRUTH.json','canonicalBoards2026.json'].includes(x.file));keep.push({file:'MODEL_SOURCE_OF_TRUTH.json',reason:structuralReason,source:structuralSource});keep.push({file:'canonicalBoards2026.json',reason:structuralReason,source:structuralSource});W('guardrails/structural-change-manifest.json',{version:'1.4.0',purpose:`Explicit declarations for intentional protected structural-file changes. Current declarations cover ${purposeSuffix}.`,changes:keep});
console.log(JSON.stringify({result:'PASS',mode:calibration,exact_model_declarations:model_changes.length,declared_players:model_changes.map(x=>({player:x.player,fields:Object.keys(x.expected_values)})),spreadsheet_sync_rows:canonical.overall.length},null,2));
