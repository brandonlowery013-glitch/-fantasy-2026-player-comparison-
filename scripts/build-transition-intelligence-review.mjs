import fs from 'node:fs';
import path from 'node:path';

const root=process.cwd();
const read=p=>JSON.parse(fs.readFileSync(path.join(root,p),'utf8'));
const write=(p,x)=>{const f=path.join(root,p);fs.mkdirSync(path.dirname(f),{recursive:true});fs.writeFileSync(f,JSON.stringify(x,null,2)+'\n');};
const source=read('MODEL_SOURCE_OF_TRUTH.json');
const ledger=read('guardrails/current-football-review.json');
const phaseConfig=read(`config/season-phase-${source.season||2026}.json`);
const expected=Number(source.active_player_model),shards=Number(source.runtime_player_shards);
const lookbackStart=process.env.TRANSITION_LOOKBACK_START||phaseConfig.retroactive_camp.start;
const lookbackEnd=process.env.TRANSITION_LOOKBACK_END||new Date().toISOString();
const startMs=Date.parse(lookbackStart),endMs=Date.parse(lookbackEnd);
if(!Number.isFinite(startMs)||!Number.isFinite(endMs)||endMs<startMs)throw new Error('Invalid transition intelligence lookback window');
const norm=s=>String(s||'').toLowerCase().normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/\b(jr|sr|ii|iii|iv)\b/g,'').replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim();
const canonTeam=x=>({'LAR':'LA','WSH':'WAS','JAC':'JAX'}[String(x||'').toUpperCase()]||String(x||'').toUpperCase());
const teamMap={'Arizona Cardinals':'ARI','Atlanta Falcons':'ATL','Baltimore Ravens':'BAL','Buffalo Bills':'BUF','Carolina Panthers':'CAR','Chicago Bears':'CHI','Cincinnati Bengals':'CIN','Cleveland Browns':'CLE','Dallas Cowboys':'DAL','Denver Broncos':'DEN','Detroit Lions':'DET','Green Bay Packers':'GB','Houston Texans':'HOU','Indianapolis Colts':'IND','Jacksonville Jaguars':'JAX','Kansas City Chiefs':'KC','Las Vegas Raiders':'LV','Los Angeles Chargers':'LAC','Los Angeles Rams':'LA','Miami Dolphins':'MIA','Minnesota Vikings':'MIN','New England Patriots':'NE','New Orleans Saints':'NO','New York Giants':'NYG','New York Jets':'NYJ','Philadelphia Eagles':'PHI','Pittsburgh Steelers':'PIT','San Francisco 49ers':'SF','Seattle Seahawks':'SEA','Tampa Bay Buccaneers':'TB','Tennessee Titans':'TEN','Washington Commanders':'WAS'};
const teamNameByAbbr=new Map(Object.entries(teamMap).map(([name,abbr])=>[abbr,name]));
let players=[];for(let i=0;i<shards;i++)players.push(...read(`players${i}.json`));
if(players.length!==expected)throw new Error(`Universe mismatch ${players.length}/${expected}`);
const reviewByName=new Map((ledger.players||[]).map(x=>[x.player,x]));

const categories={scheme_install:/\b(offense|offensive coordinator|coordinator|scheme|system|playbook|install|installation|terminology|concept|motion|under center|shotgun|rpo|play action|protection|progression|reads?)\b/i,adaptation:/\b(adapt|adjust|adjusting|comfortable|comfort|learning|learned|command|master|grasp|processing|timing|rhythm|decision making|footwork)\b/i,role_usage:/\b(role|usage|first team|starter|starting|reps|snap|route|target|carry|touch|workload|third down|two minute|red zone|goal line|slot|outside|backfield|motion)\b/i,chemistry:/\b(chemistry|rapport|connection|trust|timing|sync|communication|favorite target|working with|relationship)\b/i,competition:/\b(competition|battle|depth chart|competing|ahead of|behind|split|committee|timeshare|rotate|rotation)\b/i,readiness:/\b(healthy|health|recovery|recover|return|practice|limited|full participant|conditioning|ready|rust|sharp|explosive|speed|burst)\b/i,prior_season_injury_recovery:/\b(last season|previous season|season ending|returning from|coming back from|rehab|rehabilitation|surgery|surgical|acl|mcl|lcl|meniscus|achilles|hamstring|ankle|knee|shoulder|foot|hip|back injury|fracture|torn|tear|repair|cleared for contact|ramp up|ramping up|workload restriction|recovery timeline|setback|recurrence)\b/i,development:/\b(improv|develop|growth|step forward|breakout|polish|refine|mechanics|accuracy|vision|route running|blocking|pass protection)\b/i,teammate_environment:/\b(quarterback|qb|running back|receiver|wide receiver|tight end|offensive line|line|teammate|addition|departure|signed|traded|released)\b/i};
const operationalNoise=/\b(flight|travel|arrive in|stadium|venue|ticket|international game|melbourne|london|uniform|schedule release)\b/i;
const materiallyFootball=/\b(offense|scheme|playbook|role|usage|reps|starter|practice|chemistry|target|route|carry|touch|workload|quarterback|qb|coordinator|competition|depth chart|red zone|goal line|third down|two minute|healthy|return|recovery|rehab|surgery|acl|mcl|lcl|meniscus|achilles|hamstring|ankle|knee|shoulder|foot|hip|fracture|tear|cleared|ramp up|setback|recurrence|development|improve|timing|protection|progression|motion|under center|shotgun|rpo|play action)\b/i;
async function getJson(url){const r=await fetch(url,{headers:{'user-agent':'fantasy-2026-transition-intelligence/1.2'}});if(!r.ok)throw new Error(`${url} -> ${r.status}`);return r.json();}
function dateOf(a){return a.published||a.lastModified||a.date||a.publishedAt||a.publishDate||a.publishedDate||a.lastModifiedDate||a.created||a.createdAt||null;}
function parseNews(payload,sourceName,team=null){const out=[],seen=new WeakSet();const add=a=>{if(!a||typeof a!=='object'||seen.has(a))return;seen.add(a);const headline=a.headline||a.title||a.name||'';const description=a.description||a.summary||a.story||a.teaser||'';const published=dateOf(a);const url=a.links?.web?.href||a.link||a.url||a.webUrl||null;const body=typeof(a.story||a.content||a.body)==='string'?(a.story||a.content||a.body):'';if(headline||description||body)out.push({source:sourceName,team,headline:String(headline),description:String(description),published,url,body_text:String(body)});};const walk=x=>{if(Array.isArray(x)){x.forEach(walk);return;}if(!x||typeof x!=='object')return;add(x);for(const[k,v]of Object.entries(x))if(v&&typeof v==='object'&&!['links','images','image','video'].includes(k))walk(v);};walk(payload);const keys=new Set();return out.filter(x=>{const k=x.url||`${x.source}|${x.headline}|${x.description}`;if(keys.has(k))return false;keys.add(k);return true;});}
function withinWindow(item){const t=Date.parse(item.published);return Number.isFinite(t)&&t>=startMs&&t<=endMs+86400000;}
function text(item){return norm(`${item.headline||''} ${item.description||''} ${item.body_text||''}`);}
function classify(txt){return Object.entries(categories).filter(([,re])=>re.test(txt)).map(([k])=>k);}
function directPlayerMatch(player,item){const phrase=norm(player);const t=text(item);return phrase&&t.includes(phrase);}
function relevantEvidence(player,team,item,bound=false){if(!withinWindow(item))return null;const t=text(item);if(!materiallyFootball.test(t)||operationalNoise.test(t))return null;const cats=classify(t);if(!cats.length)return null;const direct=bound||directPlayerMatch(player,item);const teamContext=item.team===team||t.includes(norm(teamNameByAbbr.get(team)||''));if(!direct&&!teamContext)return null;return{source:item.source,team:item.team||team,published:item.published||null,url:item.url||null,headline:item.headline||null,description:item.description||null,direct_player_evidence:direct,team_context_only:!direct,categories:cats};}
async function teamDirectory(){const j=await getJson('https://site.api.espn.com/apis/site/v2/sports/football/nfl/teams?limit=40');const out=new Map();for(const row of j.sports?.[0]?.leagues?.[0]?.teams||[]){const t=row.team||row,abbr=canonTeam(t.abbreviation);if(abbr&&t.id)out.set(abbr,String(t.id));}return out;}
const teams=await teamDirectory();if(teams.size!==32)throw new Error(`Team directory incomplete ${teams.size}/32`);
const teamNews=new Map();const teamFailures=[];
for(const [abbr,id] of teams){try{teamNews.set(abbr,parseNews(await getJson(`https://site.api.espn.com/apis/site/v2/sports/football/nfl/teams/${id}/news?limit=100`),'ESPN_TEAM_TRANSITION',abbr));}catch(e){teamFailures.push(`${abbr}: ${e.message}`);teamNews.set(abbr,[]);}}
if(teamFailures.length>4)throw new Error(`Too many team-news failures: ${teamFailures.join(' | ')}`);
const rows=[];const playerFailures=[];
for(let i=0;i<players.length;i+=8){const batch=players.slice(i,i+8);const results=await Promise.all(batch.map(async p=>{const review=reviewByName.get(p.n)||{};const team=teamMap[p.t];if(!team)return{p,error:`unknown team ${p.t}`};const athleteId=review.espn_athlete_id||null;let individual=[];if(athleteId){try{individual=parseNews(await getJson(`https://site.api.espn.com/apis/site/v2/sports/football/nfl/athletes/${athleteId}/news?limit=100`),'ESPN_PLAYER_TRANSITION',team);}catch(e){playerFailures.push(`${p.n}: ${e.message}`);}}const evidence=[];for(const item of individual){const e=relevantEvidence(p.n,team,item,true);if(e)evidence.push(e);}for(const item of teamNews.get(team)||[]){const e=relevantEvidence(p.n,team,item,false);if(e)evidence.push(e);}const seen=new Set();const dedup=evidence.filter(e=>{const k=e.url||`${e.source}|${e.headline}`;if(seen.has(k))return false;seen.add(k);return true;});const direct=dedup.filter(x=>x.direct_player_evidence),teamOnly=dedup.filter(x=>x.team_context_only);const recovery=direct.filter(x=>(x.categories||[]).includes('prior_season_injury_recovery'));const covered=new Set(dedup.flatMap(x=>x.categories));return{p,row:{player:p.n,pos:p.p,team,review_status:'REVIEWED',lookback_start:lookbackStart,lookback_end:lookbackEnd,direct_evidence_count:direct.length,team_context_count:teamOnly.length,prior_season_injury_recovery_evidence_count:recovery.length,prior_season_injury_recovery_status:recovery.length?'RECOVERY_EVIDENCE_FOUND':'NO_RECOVERY_EVIDENCE_FOUND',categories_covered:[...covered],development_evidence:dedup.slice(0,50),transition_signal:direct.length||teamOnly.length?'EVIDENCE_FOUND':'REVIEWED_NO_EVIDENCE',model_action:'REVIEW_ONLY_NO_AUTOMATIC_RANK_CHANGE'}};}));for(const r of results){if(r.error)throw new Error(`${r.p.n}: ${r.error}`);rows.push(r.row);}}
if(rows.length!==expected||new Set(rows.map(x=>x.player)).size!==expected)throw new Error(`Transition coverage failure ${rows.length}/${expected}`);
const missingReview=players.map(p=>p.n).filter(n=>!reviewByName.has(n));if(missingReview.length)throw new Error(`Current football ledger missing active players: ${missingReview.join(', ')}`);
for(const row of rows){const r=reviewByName.get(row.player);r.transition_intelligence={review_status:row.review_status,lookback_start:row.lookback_start,lookback_end:row.lookback_end,direct_evidence_count:row.direct_evidence_count,team_context_count:row.team_context_count,prior_season_injury_recovery_evidence_count:row.prior_season_injury_recovery_evidence_count,prior_season_injury_recovery_status:row.prior_season_injury_recovery_status,categories_covered:row.categories_covered,transition_signal:row.transition_signal,model_action:row.model_action,evidence:row.development_evidence};}
ledger.transition_intelligence_schema={version:'1.2.0',scope:'FULL_ACTIVE_UNIVERSE',mandatory:true,lookback_start:lookbackStart,lookback_end:lookbackEnd,season_phase_config:`config/season-phase-${source.season||2026}.json`,retroactive_camp_window:phaseConfig.retroactive_camp,current_season_window:phaseConfig.current_season,categories:Object.keys(categories),rule:'PROACTIVE REVIEW REQUIRED; CAMP IS RETROACTIVE CLOSED HISTORY, CURRENT-SEASON EVIDENCE IS CURRENT-STATE AUTHORITY; PRIOR-SEASON INJURY RECOVERY MUST BE EXPLICITLY CHECKED',generated_at:new Date().toISOString()};
write('guardrails/current-football-review.json',ledger);
const counts={reviewed:rows.length,evidence_found:rows.filter(x=>x.transition_signal==='EVIDENCE_FOUND').length,no_evidence:rows.filter(x=>x.transition_signal==='REVIEWED_NO_EVIDENCE').length,direct_evidence_players:rows.filter(x=>x.direct_evidence_count>0).length,team_context_players:rows.filter(x=>x.team_context_count>0).length,prior_season_injury_recovery_players:rows.filter(x=>x.prior_season_injury_recovery_evidence_count>0).length};
const report={schema_version:'1.2.0',generated_at:new Date().toISOString(),authoritative:false,mutation_policy:'REVIEW_ONLY_NO_AUTOMATIC_RANK_CHANGE',universe:{players:expected,shards},lookback:{start:lookbackStart,end:lookbackEnd},season_phase:{retroactive_camp:phaseConfig.retroactive_camp,current_season:phaseConfig.current_season},required_categories:Object.keys(categories),source_quality:{teams:teams.size,team_news_failures:teamFailures,player_news_failures:playerFailures,date_fields_supported:['published','lastModified','date','publishedAt','publishDate','publishedDate','lastModifiedDate','created','createdAt']},counts,rows};
write('analysis/transition-intelligence-current.json',report);
const md=['# Full-Universe Transition Intelligence Backfill','',`Generated: ${report.generated_at}`,`Lookback: ${lookbackStart} → ${lookbackEnd}`,`Retroactive camp: ${phaseConfig.retroactive_camp.start} → ${phaseConfig.retroactive_camp.end}`,`Current-season authority starts: ${phaseConfig.current_season.start}`,`Coverage: ${rows.length}/${expected}`,`Evidence found: ${counts.evidence_found} | Reviewed no evidence: ${counts.no_evidence}`,`Prior-season injury recovery evidence: ${counts.prior_season_injury_recovery_players} players`,'','| Player | Team | Signal | Direct | Team context | Prior-season recovery | Categories |','|---|---|---|---:|---:|---|---|'];for(const x of rows)md.push(`| ${x.player} | ${x.team} | ${x.transition_signal} | ${x.direct_evidence_count} | ${x.team_context_count} | ${x.prior_season_injury_recovery_status} | ${x.categories_covered.join(', ')} |`);fs.writeFileSync(path.join(root,'analysis/transition-intelligence-current.md'),md.join('\n')+'\n');
console.log(JSON.stringify({result:'PASS',coverage:rows.length,lookback:report.lookback,season_phase:report.season_phase,counts,player_news_failures:playerFailures.length,team_news_failures:teamFailures.length},null,2));
