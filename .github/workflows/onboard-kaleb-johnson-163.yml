name: DEPRECATED - Legacy 162 to 166 onboarding

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Block obsolete hard-coded migration path
        run: |
          echo 'This one-off Kaleb Johnson/Corey Kiner/Tank Dell/Jonnu Smith migration is retired.'
          echo 'Use the Canonical Player Admissions workflow and admissions/queue.json.'
          exit 1
