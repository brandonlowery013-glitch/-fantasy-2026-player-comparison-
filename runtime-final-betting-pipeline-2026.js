(()=>{
  const RAW='https://raw.githubusercontent.com/brandonlowery013-glitch/-fantasy-2026-player-comparison-/main/';
  let lastSig='';
  const esc=v=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
  const pct=v=>Number.isFinite(Number(v))?`${(Number(v)*100).toFixed(1)}%`:'—';
  function state(panel,title,body){if(panel)panel.innerHTML=`<div class="empty" data-state="WAITING"><strong>${esc(title)}</strong>${esc(body)}</div>`;}
  function propsPanel(feed){const panel=document.querySelector('#games [data-game-panel="props"]');if(!panel)return;if(feed.status!=='READY'){state(panel,'Player props waiting',feed.status==='WAITING_FOR_CURRENT_WEEK_OUTPUTS'?'Current-week betting outputs are still syncing. Prior-week props are intentionally hidden.':'Waiting for the verified current-week schedule and player-prop markets.');return}const rows=(feed.props||[]).slice(0,30);if(!rows.length){state(panel,'No current prop picks','No current player prop clears the locked standalone betting threshold.');return}panel.innerHTML=`<div class="card"><div class="ctdSectionTitle"><div><div class="ctdEyebrow">WEEK ${esc(feed.week)} · PLAYER PROPS</div><h2>Model Prop Picks</h2></div></div><div class="tableWrap"><table class="table"><thead><tr><th>Player</th><th>Market</th><th>Book</th><th>Price</th><th>Edge</th><th>EV</th><th>Confidence</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${esc(x.player)}</td><td>${esc(`${x.side} ${x.line} ${x.stat}`)}</td><td>${esc(x.book)}</td><td>${esc(x.odds??'—')}</td><td>${esc(pct(x.probability_edge))}</td><td>${esc(pct(x.expected_value))}</td><td>${esc(x.confidence||'—')}</td></tr>`).join('')}</tbody></table></div></div>`;}
  function parlaysPanel(feed){const panel=document.querySelector('#games [data-game-panel="parlays"]');if(!panel)return;if(feed.status!=='READY'){state(panel,'Parlays waiting','Current-week standalone legs must be ready before parlay analysis can populate. Prior-week tickets are never carried forward.');return}const rows=(feed.parlays||[]).slice(0,20);panel.innerHTML=`<div class="ctdParlayModes"><div class="ctdFeatureCard"><h3>Model Picks</h3><p>${rows.length?`${rows.length} current Week ${esc(feed.week)} model-qualified ticket${rows.length===1?'':'s'} available.`:'No current ticket clears the model rules yet.'}</p></div><div class="ctdFeatureCard"><h3>Build Your Own</h3><p>Ticket-analysis internals are connected to the current-week leg pool; interactive ticket controls are exposed only when current validated legs are available.</p></div></div>${rows.length?`<div class="card" style="margin-top:10px"><div class="tableWrap"><table class="table"><thead><tr><th>Ticket</th><th>Legs</th><th>Book</th><th>Hit Prob</th><th>EV</th><th>Grade</th><th>Correlation</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${esc(x.ticket_id)}</td><td>${esc(x.leg_count)}</td><td>${esc(x.sportsbook)}</td><td>${esc(pct(x.model_hit_probability))}</td><td>${esc(pct(x.parlay_expected_value))}</td><td>${esc(x.grade||'—')}</td><td>${esc(x.correlation_status||'—')}</td></tr>`).join('')}</tbody></table></div></div>`:''}`;}
  function banner(feed){const el=document.getElementById('ctdGameTicker');if(!el)return;const head=el.querySelector('.ctdTickerHead span');if(head)head.textContent=feed.week?`Week ${feed.week} · Score · Spread · O/U · Model Edge`:'Score · Spread · O/U · Model Edge';}
  function apply(feed){window.CTD_BETTING_FEED=feed;if(typeof SCHEDULE!=='undefined')SCHEDULE={...(SCHEDULE||{}),week:feed.week,games:Array.isArray(feed.games)?feed.games:[]};propsPanel(feed);parlaysPanel(feed);banner(feed);document.documentElement.dataset.bettingFeedWeek=feed.week??'waiting';document.documentElement.dataset.bettingFeedStatus=feed.status||'UNKNOWN';window.dispatchEvent(new CustomEvent('ctd:betting-feed-updated',{detail:{week:feed.week,status:feed.status}}));}
  async function load(){try{const feed=await fetch(`${RAW}data/market/final-betting-ui-feed-2026.json?ts=${Date.now()}`,{cache:'no-store'}).then(r=>{if(!r.ok)throw Error(`feed ${r.status}`);return r.json()});const sig=JSON.stringify([feed.week,feed.status,feed.generated_at,feed.games?.length,feed.props?.length,feed.parlays?.length]);if(sig!==lastSig){lastSig=sig;apply(feed)}}catch(e){console.error('Final betting feed unavailable',e);const p=document.querySelector('#games [data-game-panel="props"]'),q=document.querySelector('#games [data-game-panel="parlays"]');state(p,'Betting feed unavailable','The verified current-week feed could not be loaded. No placeholder lines or prior-week picks are being shown.');state(q,'Betting feed unavailable','The verified current-week feed could not be loaded.');}}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',load,{once:true});else load();
  setInterval(load,60000);
})();
